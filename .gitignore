node_modules
dist
.env*.local

# Financial data - never commit
account-statements/
data/
*.db
*.pdf

# Claude config - may contain API keys
.claude/
CLAUDE.md
.playwright-mcp/

# macOS Finder metadata — noise, and it blocks rebases when it churns
.DS_Store
